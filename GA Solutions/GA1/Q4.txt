Use the async/await pattern to fetch JSON data from the variable url. The JSON structure looks like this: { "data": [ { "number": <number> } ] }. Your task is to calculate the total by summing all the number values inside data[i].number. Finally, return that sum as a numeric result. Only provide the JavaScript function body—no function, async, or arrow syntax. Make sure to use return for the final output
